{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "bunVersion": "1.x",
    "installCommand": "bun install",
    "buildCommand": "bun build ./api/entry.ts --outfile ./api/index.js --target bun --format esm",
    "outputDirectory": ".",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}