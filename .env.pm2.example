# ============================================================================
# Environment Configuration for PM2 Deployment
# ============================================================================
# Copy this file to .env on your VPS at /var/www/elysia-kit/shared/.env
# 
# For local PM2 testing, copy to .env in project root
# ============================================================================

# Application Settings
NODE_ENV=production
APP_PORT=3000
APP_NAME=elysia-kit
APP_URL=https://your-domain.com
LOG_LEVEL=info

# Database Configuration
# 
# Option 1: Managed PostgreSQL (Recommended for production)
# - Neon: https://neon.tech
# - Supabase: https://supabase.com
# - Railway: https://railway.app
# 
# Example connection string format:
# DATABASE_URL=postgresql://username:password@host:5432/database_name
DATABASE_URL=postgresql://user:password@localhost:5432/elysia_kit

# Email Service (Resend)
# Get your API key at: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
RESEND_MAIL=noreply@your-domain.com

# Trigger.dev (Optional - for background jobs)
# Get credentials at: https://cloud.trigger.dev
TRIGGER_SECRET_KEY=tr_dev_xxxxxxxxxxxxxxxxxxxxx
TRIGGER_PROJECT_ID=proj_xxxxxxxxxxxxxxxxxxxxx

# Observability (Optional - for local telemetry stack)
# Only needed if running Docker Compose observability stack locally
# LOKI_URL=http://localhost:3100
# TEMPO_URL=http://localhost:4318
# METRICS_EXPORTER_PORT=9464

# Observability Port Configuration (Optional - customize if defaults conflict)
# These are used when running `docker compose up` for local observability
# 
# TEMPO_UI_PORT=3200          # Tempo UI (default: 3200)
# TEMPO_OTLP_GRPC_PORT=4317   # Tempo OTLP gRPC (default: 4317)
# TEMPO_OTLP_HTTP_PORT=4318   # Tempo OTLP HTTP (default: 4318)
# PROMETHEUS_PORT=9090        # Prometheus (default: 9090)
# LOKI_PORT=3100              # Loki (default: 3100)
# GRAFANA_PORT=3001           # Grafana (default: 3001)
# METRICS_EXPORTER_PORT=9464  # App metrics endpoint (default: 9464)

# PM2 Deployment Settings (for local machine only - not needed on VPS)
# Uncomment and configure these on your LOCAL machine for PM2 deploy command
# 
# DEPLOY_HOST=your-vps-ip-or-domain
# DEPLOY_USER=deploy
# DEPLOY_KEY=~/.ssh/id_rsa
# DEPLOY_REPO=git@github.com:your-username/elysia-kit.git

# Arcjet Security (Optional - for bot protection, rate limiting, WAF)
# Get your key from https://app.arcjet.com
# ARCJET_KEY=ajkey_xxxxxxxxxxxxxxxx
# ARCJET_ENV=development  # Use 'production' for live blocking
